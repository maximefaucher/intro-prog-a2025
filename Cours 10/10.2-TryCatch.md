
# 🛠️ Gestion des exceptions en Java avec try-catch

## 🔹 Qu'est-ce qu'une exception ?

Une **exception** est un événement qui perturbe le déroulement normal d'un programme. En Java, les exceptions sont des objets qui décrivent une erreur.

## 🔹 Structure d'un bloc try-catch

```java
try {
    // Code susceptible de générer une exception
} catch (ExceptionType nomVariable) {
    // Code exécuté en cas d'exception
}
```

On peut également utiliser plusieurs blocs `catch` pour gérer différents types d'exceptions :

```java
try {
    // Code à risque
} catch (IOException e) {
    System.out.println("Erreur d'entrée/sortie : " + e.getMessage());
} catch (ArrayIndexOutOfBoundsException e) {
    System.out.println("Indice de tableau invalide.");
} catch (Exception e) {
    System.out.println("Autre exception : " + e.getMessage());
}
```

## 🔹 Les exceptions courantes en Java

### 1. `IOException`
- Se produit lors d'une erreur d'entrée/sortie.
- Exemple : lecture ou écriture dans un fichier inaccessible.

### 2. `FileNotFoundException`
- Sous-classe de `IOException`.
- Se produit lorsque le fichier spécifié n'existe pas.

### 3. `ArrayIndexOutOfBoundsException`
- Se produit lorsqu'on tente d'accéder à un indice invalide dans un tableau.

### 4. `NullPointerException`
- Se produit lorsqu'on tente d'utiliser une référence null.

### 5. `ArithmeticException`
- Se produit lors d'une opération mathématique invalide (ex : division par zéro).

## 🔹 Exemple complet

```java
import java.io.*;

public class ExempleExceptions {
    public static void main(String[] args) {
        try {
            String[] noms = {"Alice", "Bob"};
            System.out.println(noms[2]); // ArrayIndexOutOfBoundsException

            BufferedReader reader = new BufferedReader(new FileReader("fichier.txt"));
            String ligne = reader.readLine();
            reader.close();
        } catch (FileNotFoundException e) {
            System.out.println("Fichier introuvable : " + e.getMessage());
        } catch (IOException e) {
            System.out.println("Erreur d'E/S : " + e.getMessage());
        } catch (ArrayIndexOutOfBoundsException e) {
            System.out.println("Indice hors limites : " + e.getMessage());
        }
    }
}
```

## 🔹 Bonnes pratiques

- Toujours fermer les ressources (fichiers, flux).
- Utiliser des blocs `catch` spécifiques avant les génériques.
- Utiliser `finally` si nécessaire pour garantir l'exécution d'un bloc de code.

```java
try {
    // Code
} catch (Exception e) {
    // Gestion
} finally {
    // Toujours exécuté
}
```
