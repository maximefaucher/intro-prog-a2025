
### 📘 Lecture et écriture de fichiers en Java

#### 🔹 Concepts de base

- Un **fichier** est une ressource externe contenant des données persistantes.
- En Java, on peut lire et écrire des fichiers en utilisant les classes du package `java.io`.

#### 🔹 Écriture dans un fichier

```java
import java.io.FileWriter;
import java.io.IOException;

public class EcritureFichier {
    public static void main(String[] args) {
        try {
            FileWriter writer = new FileWriter("exemple.txt");
            writer.write("Bonjour, monde !");
            writer.close();
        } catch (IOException e) {
            System.out.println("Erreur lors de l'écriture : " + e.getMessage());
        }
    }
}
```

**Points clés :**
- `FileWriter` permet d’écrire du texte dans un fichier.
- Il faut toujours **fermer** le fichier avec `close()` pour libérer les ressources.

#### 🔹 Lecture d’un fichier ligne par ligne

```java
import java.io.BufferedReader;
import java.io.FileReader;
import java.io.IOException;

public class LectureFichier {
    public static void main(String[] args) {
        try {
            BufferedReader reader = new BufferedReader(new FileReader("exemple.txt"));
            String ligne;
            while ((ligne = reader.readLine()) != null) {
                System.out.println(ligne);
            }
            reader.close();
        } catch (IOException e) {
            System.out.println("Erreur lors de la lecture : " + e.getMessage());
        }
    }
}
```

**Points clés :**
- `BufferedReader` lit le fichier **ligne par ligne**.
- `readLine()` retourne `null` à la fin du fichier.

---

### 📊 Lecture de fichiers CSV

#### 🔹 Qu’est-ce qu’un fichier CSV ?

- CSV = **Comma-Separated Values** (valeurs séparées par des virgules).
- Chaque ligne représente un **enregistrement**.
- Chaque valeur est une **colonne**.

Exemple :
```
Nom,Âge,Ville
Alice,30,Shawinigan
Bob,25,Gatineau
```

#### 🔹 Lecture d’un fichier CSV sans bibliothèques externes

```java
import java.io.BufferedReader;
import java.io.FileReader;
import java.io.IOException;

public class LectureCSV {
    public static void main(String[] args) {
        try {
            BufferedReader reader = new BufferedReader(new FileReader("donnees.csv"));
            String ligne;
            while ((ligne = reader.readLine()) != null) {
                String[] colonnes = ligne.split(",");
                System.out.println("Nom : " + colonnes[0]);
                System.out.println("Âge : " + colonnes[1]);
                System.out.println("Ville : " + colonnes[2]);
                System.out.println("-----");
            }
            reader.close();
        } catch (IOException e) {
            System.out.println("Erreur : " + e.getMessage());
        }
    }
}
```

**Points clés :**
- `split(",")` permet de séparer les valeurs.
- Attention aux **valeurs contenant des virgules** entre guillemets (cas avancé).

---

