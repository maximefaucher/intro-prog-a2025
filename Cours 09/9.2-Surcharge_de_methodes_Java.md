# 💡 La surcharge de méthodes en Java

## 🎯 Objectif
Comprendre ce qu’est **la surcharge de méthodes** (ou *method overloading*) en Java, et comment l’utiliser dans un programme simple.

---

## 🧩 Qu’est-ce qu’une méthode?

En Java, une **méthode** est un bloc de code qu’on peut exécuter en l’appelant par son nom.

Exemple :
```java
public static void direBonjour() {
    System.out.println("Bonjour!");
}
```

Ici, la méthode s’appelle `direBonjour`.  
Elle ne prend **aucun paramètre** et affiche simplement le texte `"Bonjour!"`.

---

## 🔁 Qu’est-ce que la surcharge de méthodes?

La **surcharge de méthodes** veut dire qu’on peut **avoir plusieurs méthodes avec le même nom**, **mais avec des paramètres différents**.

👉 En d’autres mots :  
> On peut “réutiliser” le même nom de méthode, à condition que le **nombre** ou le **type** des paramètres change.

C’est le **compilateur Java** qui choisit automatiquement **quelle version** de la méthode exécuter selon les arguments qu’on lui envoie.

---

## 📘 Exemple simple

Voici un exemple avec la méthode `afficherMessage()` utilisée plusieurs fois :

```java
public class ExempleSurcharge {

    public static void main(String[] args) {
        afficherMessage();
        afficherMessage("Salut!");
        afficherMessage("Bonjour", 3);
    }

    // Méthode sans paramètre
    public static void afficherMessage() {
        System.out.println("Message par défaut");
    }

    // Méthode avec un paramètre String
    public static void afficherMessage(String message) {
        System.out.println(message);
    }

    // Méthode avec un String et un int
    public static void afficherMessage(String message, int nbFois) {
        for (int i = 0; i < nbFois; i++) {
            System.out.println(message);
        }
    }
}
```

### 🧠 Que fait ce programme?
1. `afficherMessage();` → affiche `Message par défaut`
2. `afficherMessage("Salut!");` → affiche `Salut!`
3. `afficherMessage("Bonjour", 3);` → affiche `Bonjour` trois fois

---

## ⚙️ Comment Java choisit la bonne méthode?

Quand tu appelles une méthode, Java regarde :
1. Le **nom** de la méthode
2. Le **nombre de paramètres**
3. Le **type de chaque paramètre**

S’il trouve une méthode qui correspond, c’est celle-là qu’il exécute.

Exemple :
```java
afficherMessage(123);
```
→ Java cherchera une méthode `afficherMessage(int nombre)`.

---

## 🚫 Ce qu’on **ne peut pas** faire

Tu **ne peux pas** surcharger une méthode uniquement avec un **type de retour différent**.  
Par exemple :
```java
public static void calculer() {}
public static int calculer() { return 0; } // ❌ Erreur!
```

➡️ Les deux ont le même nom et les mêmes paramètres.  
Java ne peut pas les distinguer, donc il ne sait pas laquelle exécuter.

---

## 🧪 Autre exemple pratique

Voici un exemple avec des calculs simples :

```java
public class Calculatrice {

    public static void main(String[] args) {
        System.out.println(addition(5, 10));          // Appelle la version int
        System.out.println(addition(2.5, 3.7));       // Appelle la version double
        System.out.println(addition(1, 2, 3));        // Appelle la version à 3 paramètres
    }

    public static int addition(int a, int b) {
        return a + b;
    }

    public static double addition(double a, double b) {
        return a + b;
    }

    public static int addition(int a, int b, int c) {
        return a + b + c;
    }
}
```

### Résultat :
```
15
6.2
6
```

---

## ✅ En résumé

| Concept | Description |
|----------|--------------|
| **Nom identique** | Oui, c’est permis |
| **Paramètres différents** | Obligatoire (nombre ou type) |
| **Type de retour différent seulement** | ❌ Non permis |
| **Choix de la méthode** | Fait automatiquement par le compilateur |

---

## 🏁 En bref
La surcharge de méthodes permet d’utiliser le **même nom** de méthode pour plusieurs tâches **similaires**, sans avoir à inventer des noms différents comme `additionInt`, `additionDouble`, etc.  
C’est une manière de **rendre ton code plus clair et plus réutilisable**.

---

✍️ **Exercice proposé**
1. Crée une méthode `public static void afficherInfo()` sans paramètre qui affiche ton nom.
2. Surcharge-la avec une version `afficherInfo(String cours)` qui affiche ton nom et le nom du cours.
3. Surcharge encore avec une version `afficherInfo(String cours, int session)` qui affiche ton nom, le cours et la session.
4. Appelle-les toutes dans `main()` et observe le résultat.
